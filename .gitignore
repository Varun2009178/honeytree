.DS_Store
*.pem
node_modules/
.superpowers/
.npmrc
.honeytree/
*.vsix
**/docs/superpowers/
.claude/

# Environment / secrets (anywhere in the repo) — NEVER commit real secrets.
# This is the only privacy boundary that matters: app code references
# process.env.*, but the actual values live only in .env.local (ignored below).
.env
.env.*
**/.env
**/.env.*
